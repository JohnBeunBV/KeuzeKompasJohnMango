services:
  model-api:
    build: ./python-model
    volumes:
      - ./models:/models
      - ./certs:/certs
    environment:
      - API_KEY=${MODEL_API_KEY}
    ports:
      - "8443:${MODEL_API_PORT}"
