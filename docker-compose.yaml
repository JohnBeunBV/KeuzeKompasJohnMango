services:
  model-api:
    build: ./python-model
    volumes:
      - ./models:/models
      - ./certs:/certs
    environment:
      - PYTHON_API_KEY=${PYTHON_API_KEY}
    ports:
      - "${MODEL_API_PORT}:8000"
